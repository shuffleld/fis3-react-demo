{
  "name": "use-react",
  "version": "1.0.0",
  "description": "use-react ====================",
  "main": "fis-conf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "fis3 release && fis3 server start && fis3 release -wL",
    "build": "fis3 release -d ./dist"
  },
  "author": "shuffleld",
  "license": "ISC",
  "devDependencies": {
    "fis-parser-babel-5.x": "^1.4.0",
    "fis-parser-less": "^0.1.3",
    "fis3-hook-commonjs": "^0.1.23",
    "fis3-postpackager-loader": "^2.0.4"
  }
}
